<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Required on Android 11+ to query installed packages (Package Visibility API) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Explicitly declare brain-rot packages for Android 11+ visibility -->
    <queries>
        <package android:name="com.zhiliaoapp.musically" />    <!-- TikTok -->
        <package android:name="com.ss.android.ugc.trill" />    <!-- TikTok (alt) -->
        <package android:name="com.instagram.android" />       <!-- Instagram -->
        <package android:name="com.twitter.android" />         <!-- X (Twitter) -->
        <package android:name="com.google.android.youtube" />  <!-- YouTube -->
        <package android:name="com.reddit.frontpage" />        <!-- Reddit -->
        <package android:name="com.snapchat.android" />        <!-- Snapchat -->
        <package android:name="com.facebook.katana" />         <!-- Facebook -->
        <package android:name="com.facebook.lite" />           <!-- Facebook Lite -->
        <!-- Also allow querying all launcher intents for the app picker -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
      
      <!-- Mindlock Quiz Overlay Activity -->
      <activity
        android:name=".MindlockQuizActivity"
        android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
        android:launchMode="singleTop"
        android:excludeFromRecents="true"
        android:exported="false" />
      
      <!-- Warden Background Service -->
      <service
        android:name=".WardenService"
        android:foregroundServiceType="dataSync"
        android:exported="false" />

      <!-- Notification BroadcastReceivers -->
      <receiver
        android:name=".StreakReminderReceiver"
        android:exported="false" />
      <receiver
        android:name=".EpochAlertReceiver"
        android:exported="false" />
        
    </application>
</manifest>
